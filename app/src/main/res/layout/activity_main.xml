<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.epicodus.discussionforum.MainActivity">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add New Topic"
        android:id="@+id/addNewTopicButton"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="#000000"
        android:textColor="#ffffff"
        android:textSize="25sp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Topics"
        android:id="@+id/topicTitleTextView"
        android:layout_centerHorizontal="true"
        android:textSize="24sp" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="400dp"
        android:id="@+id/TopicListView"
        android:layout_below="@+id/topicTitleTextView"
        android:layout_alignLeft="@+id/addNewTopicButton"
        android:layout_alignStart="@+id/addNewTopicButton" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/topicEditText"
        android:layout_below="@+id/TopicListView"
        android:hint="Add New Topic"
        android:textAlignment="center" />
</RelativeLayout>
